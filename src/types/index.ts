export interface IPosition {
  x: number;
  y: number;
}

export interface ISize {
  w: number;
  h: number;
}

export interface IVelocity {
  x: number;
  y: number;
}

export enum Key {
  SPACE = ' ',
}
